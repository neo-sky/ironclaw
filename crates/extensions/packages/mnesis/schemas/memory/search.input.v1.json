{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Mnesis memory search input",
  "type": "object",
  "additionalProperties": false,
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4096,
      "description": "What to recall from prior experience in the current scope."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 20,
      "default": 10,
      "description": "Maximum records to return. The host budget is authoritative and may lower this."
    }
  }
}
